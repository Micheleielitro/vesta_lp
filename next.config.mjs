/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/vesta_lp',
  assetPrefix: '/vesta_lp/',
  images: {
    unoptimized: true,
  },
}

export default nextConfig
